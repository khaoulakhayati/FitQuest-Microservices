spring:
  datasource:
    url: jdbc:mysql://${MYSQL_NUTRITION_HOST:localhost}:${MYSQL_NUTRITION_PORT:3307}/${MYSQL_NUTRITION_DB:fitquest_nutrition}?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: ${MYSQL_NUTRITION_USER:root}
    password: ${MYSQL_NUTRITION_PASSWORD:root}
  jpa:
    hibernate:
      ddl-auto: update
